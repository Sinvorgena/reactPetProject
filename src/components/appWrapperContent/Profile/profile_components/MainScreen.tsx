import s from "./MainScreen.module.scss"
import React from "react";


let MainScreen:React.FC = () => {
    return (
        <div className={s.content}>

        </div>
    )
}

export default React.memo(MainScreen);